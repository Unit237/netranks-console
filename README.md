running with docker:
docker build -t netranks-frontend-main .
docker run -p 3000:80 netranks-frontend-main

type the brand
select the question then send that one question.
look good on mobile but when you get to console, let them know it's a desktop site.
two tokens for visitor and user.
